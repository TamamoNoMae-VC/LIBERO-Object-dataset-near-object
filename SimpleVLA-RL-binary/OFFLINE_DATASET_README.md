# SimpleVLA-RL Binary Variant

This copy is prepared to work with the LIBERO A-to-B successful-only binary dataset.
Use `offline_dataset_manifest.json` and the example scripts in `examples/` to inspect and load the data.
